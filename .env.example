# App Configuration
APP_NAME="Well (Dev)"
APP_ENV=local
APP_KEY=base64:6SaJp+C62H37hXprmVyVZPSCnMZ/3nPEYH/Tr9pkIDs=
# Enable debug mode for development
APP_DEBUG=true
# Adjust according to your local timezone
APP_TIMEZONE=UTC
# Adjust based on your Docker Compose port mapping
APP_URL=http://localhost:8000

# Language Settings
APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

# Maintenance Mode (Usually not needed in development)
# APP_MAINTENANCE_DRIVER=file

# Password Hashing Rounds
BCRYPT_ROUNDS=12

# Your BX Credentials (Development Environment)
BX_LOGIN=your_dev_bx_login
BX_KEY=your_dev_bx_api_key

# Logging Configuration (Development usually needs more verbose logs)
LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug # Set to debug or info in development for easier troubleshooting

# Database Configuration (Points to the database service name in docker-compose)
DB_CONNECTION=mysql
DB_HOST=mysql
DB_PORT=3306
DB_DATABASE=mydasa
DB_USERNAME=sail
DB_PASSWORD=password

# Session Configuration (Database is okay for dev, Redis recommended for prod)
SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

# Cache Configuration (Database is okay for dev, Redis recommended for prod)
CACHE_STORE=database
CACHE_PREFIX=

# Vite Configuration
VITE_APP_NAME=Well